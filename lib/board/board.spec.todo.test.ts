/**
 * Placeholder spec for the future real dashboard board.
 *
 * These tests document the intended behaviour once the kanban board is built
 * into the dashboard (currently only a landing-page mock exists).
 * Delete this file and implement against the real board component once it ships.
 */
import { describe, it } from 'vitest'

describe('Dashboard board — real implementation (TODO)', () => {
  it.todo('creates a new card in the focused lane with the "n" shortcut')
  it.todo('moves a card to a different lane via drag-and-drop')
  it.todo('reorders cards within a lane via drag-and-drop')
  it.todo('persists lane order after page reload')
  it.todo('persists card positions after page reload')
  it.todo('renames a lane inline')
  it.todo('deletes a lane and redistributes its cards')
  it.todo('search returns relevant cards across all lanes')
  it.todo('Escape closes an open card detail modal')
  it.todo('Enter opens the focused card in a detail modal')
})
